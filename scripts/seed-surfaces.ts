// Retired after migration 0011. Agency-owned portal surfaces are RPC-only and real Kanset data
// belongs in the private canonical/integration workflow, not public-repository demo fixtures.
console.error('seed-surfaces is retired; use npm run portal-write with reviewed client-safe input.')
process.exit(1)
